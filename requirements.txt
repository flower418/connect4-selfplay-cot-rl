# Core utilities used by data conversion and result handling.
pandas>=2.2.0
pyarrow>=15.0.0

# Hugging Face model evaluation.
torch>=2.3.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.3

# Optional but useful on servers for deterministic progress/debugging.
tqdm>=4.66.0
